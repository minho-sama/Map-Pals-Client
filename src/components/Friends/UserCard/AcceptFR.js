import React from 'react';

function AcceptFR({profile, user, notifyError, refreshUsers, setRefreshUsers, token}) {
  return (
    <div 
        className="friendController" 
        >
        accept friend request
    </div>
  )
}

export default AcceptFR;
